RESEND_API_KEY=
RESEND_FROM_EMAIL=onboarding@resend.dev
ADMIN_USER=admin
ADMIN_PASSWORD=change-me
# Optional: directory for the SQLite file (point at a mounted volume in production, e.g. /data)
DATA_DIR=
# Optional: IANA timezone for dashboard date display (defaults to America/Denver)
DISPLAY_TZ=
